"""Offline analyses over the recipe library.

Read-only jobs that grade or summarise the data rather than change it: their
output is a report or a review queue for a human, never a write to the store.
"""
