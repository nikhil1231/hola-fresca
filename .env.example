# Copy to .env (gitignored) and fill in. Used by the ingredient→product mapping
# proposal pass (app/mapping).
OPENAI_API_KEY=sk-...
# Optional: override the model used for proposals.
# HOLAFRESCA_OPENAI_MODEL=gpt-5.6-luna
