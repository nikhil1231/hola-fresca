.venv/
__pycache__/
*.py[cod]
.pytest_cache/
AGENTS.local.md

# Scrape output: SQLite DB and raw payload cache (rebuildable from the source).
# Anchored to the repo root so app/data/ (the committed gram reference) is kept.
/data/
.env
.env.*
!.env.example

frontend/node_modules/
frontend/dist/
frontend/.env
frontend/.env.*
!frontend/.env.example

# Local editor and coding-agent workspaces.
/.claude/
/.codex/
/.codex-remote-*/

# Local data exports and development-server logs.
/holafresca-ocado-data.*
/*.log
